<nav x-data="{ open: false }" class="">
    <!-- Primary Navigation Menu -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
            <div class="flex">
                <!-- Your navigation menu items here -->
            </div>

            <div class="hidden sm:flex sm:items-center sm:ml-6">
                <!-- Teams Dropdown -->
                @if (Laravel\Jetstream\Jetstream::hasTeamFeatures())
                    <!-- Your teams dropdown code here -->
                @endif

                <!-- Settings Dropdown -->
                <div class="ml-3 relative">
                    <x-dropdown align="right" width="48">
                        <x-slot name="trigger">
                            @if (Laravel\Jetstream\Jetstream::managesProfilePhotos())
                                <button class="flex text-sm border-2 border-transparent rounded-full focus:outline-none focus:border-gray-300 transition">
                                    @if (Auth::guard('receptionist')->check())
                                        <img class="h-8 w-8 rounded-full object-cover" src="{{ Auth::guard('receptionist')->user()->profile_photo_url }}" alt="{{ Auth::guard('receptionist')->user()->name }}" />
                                    @elseif (Auth::guard('web')->check())
                                        <img class="h-8 w-8 rounded-full object-cover" src="{{ Auth::user()->profile_photo_url }}" alt="{{ Auth::user()->name }}" />
                                    @elseif (Auth::guard('labassistant')->check())
                                        <img class="h-8 w-8 rounded-full object-cover" src="{{ Auth::guard('labassistant')->user()->profile_photo_url }}" alt="{{ Auth::guard('labassistant')->user()->name }}" />
                                    @elseif (Auth::guard('nurse')->check())
                                        <img class="h-8 w-8 rounded-full object-cover" src="{{ Auth::guard('labassistant')->user()->profile_photo_url }}" alt="{{ Auth::guard('nurse')->user()->name }}" />
                                    @endif
                                </button>
                            @else
                                <span class="inline-flex rounded-md">
                                    <button type="button" class="inline-flex items-center px-3 py-2 border border-transparent text-sm leading-4 font-medium rounded-md text-gray-500 bg-white hover:text-gray-700 focus:outline-none focus:bg-gray-50 active:bg-gray-50 transition ease-in-out duration-150">
                                        @if (Auth::guard('receptionist')->check())
                                            {{ Auth::guard('receptionist')->user()->name }}
                                        @elseif (Auth::guard('web')->check())
                                            {{ Auth::user()->name }}
                                        @elseif (Auth::guard('labassistant')->check())
                                            {{ Auth::guard('labassistant')->user()->name }}
                                        @elseif (Auth::guard('nurse')->check())
                                            {{ Auth::guard('nurse')->user()->name }}
                                        @endif

                                        <svg class="ml-2 -mr-0.5 h-4 w-4" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5" />
                                        </svg>
                                    </button>
                                </span>
                            @endif
                        </x-slot>

                        <x-slot name="content">
                            <!-- Account Management -->
                            <div class="block px-4 py-2 text-xs text-gray-400">
                                {{ __('Manage Account') }}
                            </div>

                            <x-dropdown-link href="{{ route('profile.show') }}">
                                {{ __('Profile') }}
                            </x-dropdown-link>

                            @if (Laravel\Jetstream\Jetstream::hasApiFeatures())
                                <x-dropdown-link href="{{ route('api-tokens.index') }}">
                                    {{ __('API Tokens') }}
                                </x-dropdown-link>
                            @endif

                            <div class="border-t border-gray-200"></div>

                            <!-- Authentication -->
                            <form method="POST" action="{{ route('logout') }}" x-data>
                                @csrf

                                <x-dropdown-link href="{{ route('logout') }}"
                                                 @click.prevent="$root.submit();">
                                    {{ __('Log Out') }}
                                </x-dropdown-link>
                            </form>
                        </x-slot>
                    </x-dropdown>
                </div>
            </div>

            <!-- Hamburger -->
            <div class="-mr-2 flex items-center sm:hidden">
                <!-- Hamburger button code here -->
            </div>
        </div>
    </div>

    <!-- Responsive Navigation Menu -->
    <div :class="{'block': open, 'hidden': ! open}" class="hidden sm:hidden">
        <!-- Responsive menu code here -->
    </div>
</nav>
